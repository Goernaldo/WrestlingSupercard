# Comic Wrestling SuperCard 0.1

Browserbasiertes Wrestling-Sammelkartenspiel als lokale React-Demo.

## Installation und Start

```bash
npm install
npm run dev
```

Produktionsprüfung: `npm run build`.

## Demo-Zugang

- Benutzer: `GörnaldoBerlin`
- Passwort: `demo123`
- Rolle: Owner

## Struktur

Gemeinsame UI liegt unter `src/components`, Seiten unter `src/pages`, lokale Daten unter `src/data` und der persistente Demo-Zustand im `src/context`.
