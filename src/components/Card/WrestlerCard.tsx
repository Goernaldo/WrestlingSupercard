import type {WrestlerCard as CardType} from '../../types/game';import styles from './WrestlerCard.module.css';
export function WrestlerCard({card,onFavorite,selected,onClick}:{card:CardType;onFavorite?:()=>void;selected?:boolean;onClick?:()=>void}){return <article className={`${styles.card} ${styles[card.frame]??''} ${selected?styles.selected:''}`} onClick={onClick}><div className={styles.art}><span>{card.name.slice(0,1)}</span><small>WRESTLER ART</small></div><div className={styles.body}><p>{card.rarity}</p><h3>{card.name}</h3><em>{card.nickname}</em><div className={styles.stats}><span>ANG {card.attack}</span><span>DEF {card.defense}</span><span>TEC {card.technique}</span><span>AUS {card.stamina}</span></div><strong>GES {card.overall}</strong>{onFavorite&&<button onClick={e=>{e.stopPropagation();onFavorite()}} aria-label="Favorit">{card.favorite?'★':'☆'}</button>}</div></article>}
