import{useGame}from'../../context/GameContext';import{Panel}from'../../components/Panel/Panel';import{UiButton}from'../../components/UiButton/UiButton';
export function Settings(){const{state,update,reset,logout}=useGame();return <div><div className="page-heading"><div><span className="tag">Account</span><h1>Ein<span>stellungen</span></h1></div></div><Panel title="Spieloptionen" eyebrow="Lokal gespeichert"><label><input type="checkbox" checked={state.settings.sound} onChange={e=>update(s=>({...s,settings:{...s.settings,sound:e.target.checked}}))}/> Soundeffekte</label><br/><label><input type="checkbox" checked={state.settings.animations} onChange={e=>update(s=>({...s,settings:{...s.settings,animations:e.target.checked}}))}/> Animationen</label><br/><br/><UiButton onClick={logout}>Abmelden</UiButton> <UiButton onClick={()=>confirm('Demo-Daten wirklich zurücksetzen?')&&reset()}>Demo-Daten zurücksetzen</UiButton></Panel></div>}
