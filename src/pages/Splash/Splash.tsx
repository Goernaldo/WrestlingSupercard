import{useEffect,useState}from'react';import{useNavigate}from'react-router-dom';import{UiButton}from'../../components/UiButton/UiButton';import styles from'./Splash.module.css';
export function Splash(){const[p,setP]=useState(0),nav=useNavigate();useEffect(()=>{const id=setInterval(()=>setP(v=>Math.min(100,v+5)),70);return()=>clearInterval(id)},[]);return <main className={styles.page}><div className={styles.arena}><span>INFERNO</span><span>BLACKOUT</span><span>THE TITAN</span></div><div className={styles.logo}><small>COMIC WRESTLING</small><h1>SUPER<span>CARD</span></h1><p>Season 1 – Version 0.1 Alpha</p></div><div className={styles.load}><progress value={p} max="100"/><b>{p}%</b>{p===100&&<UiButton onClick={()=>nav('/login')}>Arena betreten</UiButton>}</div></main>}
